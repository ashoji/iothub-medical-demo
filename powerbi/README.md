# Power BI Desktop - 医療デバイス監視ダッシュボード

Azure Blob Storage (`processed` コンテナ) のテレメトリデータを Power BI Desktop で可視化する手順です。

## 前提条件

- [Power BI Desktop](https://powerbi.microsoft.com/desktop/) がインストール済み
- `run_all_devices.sh` で 3 時間分のデータが蓄積済み（約 3,000 件以上推奨）
- ストレージアカウント: `<your-storage-account>`

## データソース情報

| 項目 | 値 |
|---|---|
| ストレージアカウント | `<your-storage-account>` |
| コンテナ | `processed` |
| ファイル形式 | JSON (1ファイル = 1レコード) |
| パス構造 | `{deviceId}/{YYYY-MM-DD}/{HH-mm-ss-ffffff}.json` |

### JSON スキーマ

```json
{
  "deviceId": "icu-device01",
  "timestamp": "2026-02-19T00:57:16Z",
  "heartRate": 111,
  "bloodPressureSystolic": 141,
  "bloodPressureDiastolic": 80,
  "bodyTemperature": 38.5,
  "spo2": 92,
  "respiratoryRate": 20,
  "patientStatus": "warning",
  "processedAt": "2026-02-19T00:57:17Z",
  "serverStatus": "warning",
  "alerts": ["heartRate:elevated", "bodyTemperature:elevated", "spo2:elevated"]
}
```

| フィールド | 型 | 説明 |
|---|---|---|
| `deviceId` | string | デバイスID (icu-device01, general-device01 等) |
| `timestamp` | datetime | デバイス側の送信時刻 (UTC) |
| `heartRate` | number | 心拍数 (bpm) |
| `bloodPressureSystolic` | number | 収縮期血圧 (mmHg) |
| `bloodPressureDiastolic` | number | 拡張期血圧 (mmHg) |
| `bodyTemperature` | number | 体温 (℃) |
| `spo2` | number | 酸素飽和度 (%) |
| `respiratoryRate` | number | 呼吸数 (回/分) |
| `patientStatus` | string | デバイス側判定 (normal/warning/critical) |
| `processedAt` | datetime | クラウド側処理時刻 (UTC) |
| `serverStatus` | string | クラウド側再判定 (normal/warning/critical) |
| `alerts` | array | アラート詳細の配列 |

---

## 手順 1: Power BI Desktop でデータを取得

### 1-1. Azure Blob Storage に接続

1. Power BI Desktop を起動
2. **「データを取得」→「Azure」→「Azure Blob Storage」** を選択
3. ストレージアカウント名を入力:
   ```
   <your-storage-account>
   ```
4. **「アカウント キー」** を選択し、以下のキーを入力:
   ```
   <your-storage-account-key>
   ```
5. 「接続」をクリック

### 1-2. コンテナを選択

1. ナビゲーターで **`processed`** コンテナにチェックを入れる
2. **「データの変換」** をクリック（「読み込み」ではなく）

### 1-3. Power Query で JSON を展開

Power Query エディターが開いたら、以下の手順でデータを整形します。

1. **不要な列を削除**: `Name` と `Content` 以外の列を削除
   - `Content` 列以外は後で不要になるが、`Name` はパス情報として残しておく

2. **`Content` 列を展開**:
   - `Content` 列のヘッダーにある展開ボタン（↓↓）をクリック
   - または: `Content` 列を右クリック →「JSON として解析」を選択
   - 展開ボタンが出たら全フィールドにチェックを入れて「OK」

3. **列名の型を設定**:

   | 列名 | 型 |
   |---|---|
   | deviceId | テキスト |
   | timestamp | 日付/時刻 (UTC) |
   | heartRate | 10進数 |
   | bloodPressureSystolic | 10進数 |
   | bloodPressureDiastolic | 10進数 |
   | bodyTemperature | 10進数 |
   | spo2 | 10進数 |
   | respiratoryRate | 10進数 |
   | patientStatus | テキスト |
   | processedAt | 日付/時刻 (UTC) |
   | serverStatus | テキスト |
   | alerts | テキスト (そのまま) |

4. **「閉じて適用」** をクリック

### 1-4. (オプション) Power Query M コード

上記が難しい場合、「詳細エディター」に以下を貼り付けてください:

```m
let
    Source = AzureStorage.Blobs("<your-storage-account>"),
    processed = Source{[Name="processed"]}[Data],
    FilteredRows = Table.SelectRows(processed, each Text.EndsWith([Name], ".json")),
    ParsedJSON = Table.AddColumn(FilteredRows, "Parsed", each Json.Document([Content])),
    Expanded = Table.ExpandRecordColumn(ParsedJSON, "Parsed", 
        {"deviceId", "timestamp", "heartRate", "bloodPressureSystolic", 
         "bloodPressureDiastolic", "bodyTemperature", "spo2", "respiratoryRate",
         "patientStatus", "processedAt", "serverStatus", "alerts"}),
    RemovedColumns = Table.RemoveColumns(Expanded, {"Name", "Content", "Extension", 
        "Date accessed", "Date modified", "Date created", "Attributes", "Folder Path"}),
    TypedColumns = Table.TransformColumnTypes(RemovedColumns, {
        {"deviceId", type text}, 
        {"timestamp", type datetimezone},
        {"heartRate", type number}, 
        {"bloodPressureSystolic", type number},
        {"bloodPressureDiastolic", type number}, 
        {"bodyTemperature", type number},
        {"spo2", type number}, 
        {"respiratoryRate", type number},
        {"patientStatus", type text}, 
        {"processedAt", type datetimezone},
        {"serverStatus", type text}, 
        {"alerts", type text}
    })
in
    TypedColumns
```

---

## 手順 2: ダッシュボードを作成

Power BI Desktop の画面構成を先に理解しておきます:

```
┌─────────────────────────────────────────────────────────────┐
│ ファイル  ホーム  挿入  モデリング  表示  ...               │  ← リボンメニュー
├───────────────────────────────────────────┬─────────────────┤
│                                           │ フィルター      │
│                                           │ ┌─────────────┐│
│          キャンバス (白い領域)              │ │ ビジュアル  ││  ← ここでグラフ種類を選ぶ
│          ここにグラフを配置する             │ │ 📊📈📉🍩  ││
│                                           │ ├─────────────┤│
│                                           │ │ フィールド   ││  ← X軸/Y軸/凡例にドラッグ
│                                           │ │ X軸: ___    ││
│                                           │ │ Y軸: ___    ││
│                                           │ │ 凡例: ___   ││
│                                           │ ├─────────────┤│
│                                           │ │ データ       ││  ← テーブルの列一覧
│                                           │ │ □ deviceId  ││
│                                           │ │ □ timestamp ││
│                                           │ │ □ heartRate ││
├───────────────────────────────────────────┤ │ □ spo2      ││
│ ページ1 │ ＋                               │ └─────────────┘│
└───────────────────────────────────────────┴─────────────────┘
```

**基本操作の流れ**: ①グラフ種類を選ぶ → ②右側のデータからフィールドをドラッグ → ③サイズ調整

---

### ページ 1: リアルタイムバイタルモニター

まず、ページ下部のタブ「ページ1」を右クリック →「名前の変更」→ **「バイタルモニター」** と入力します。

#### ビジュアル A: 心拍数の時系列推移（折れ線グラフ）

1. キャンバスの空白部分をクリック（何も選択されていない状態にする）
2. 右側の **「ビジュアルの種類」** パネルから **「折れ線グラフ」** アイコン（📈）をクリック
3. キャンバスに空のグラフが表示される
4. 右側の **「データ」** パネル（テーブルの列一覧）から:
   - **`timestamp`** をドラッグして → 「**X 軸**」エリアにドロップ
   - **`heartRate`** をドラッグして → 「**Y 軸**」エリアにドロップ
   - **`deviceId`** をドラッグして → 「**凡例**」エリアにドロップ
5. **Y 軸の集計方法を変更**: Y 軸エリアの `heartRate` の横の **▼** をクリック → **「平均」** を選択（デフォルトの「合計」だと複数デバイスの値が足し合わされ、非現実的な数値になります）
6. グラフの角をドラッグしてサイズを調整（キャンバスの左上、幅:半分、高さ:1/3 程度）

> **注意**: `timestamp` を X 軸に入れると自動的に「年 → 四半期 → 月 → 日」の階層になることがあります。
> その場合は「視覚化」パネル上部の **一番左のアイコン**（ビジュアルのビルド 📊）をクリックし、
> **「X 軸」** エリアに表示されている `日付の階層` の **▼** をクリック →**「timestamp」** を選択してください。

**参照線の追加（閾値ライン）:**
1. 作成した折れ線グラフをクリックして選択
2. 右側のパネルで **「さらに分析をビジュアルに追加する」**（虫眼鏡 🔍 のアイコン）をクリック
3. **「Y 軸の定数線」** → **「+ 行の追加」** をクリック
4. 値に **`100`** と入力 → 色を **黄色 `#F39C12`** に変更 → ラベルに「warning」と入力
5. 再度 **「+ 行の追加」** → 値に **`120`** → 色を **赤 `#E74C3C`** → ラベルに「critical」

#### ビジュアル B: 体温の時系列推移（折れ線グラフ）

1. キャンバスの **空白部分** をクリック（※重要: 既存グラフの外側をクリック）
2. 「ビジュアルの種類」から **「折れ線グラフ」** を選択
3. フィールドを設定:
   - **X 軸**: `timestamp`
   - **Y 軸**: `bodyTemperature`（▼ → **「平均」** に変更）
   - **凡例**: `deviceId`
4. 心拍数グラフの右隣に配置（幅:半分、高さ:1/3）
5. 参照線を追加:「さらに分析をビジュアルに追加する」→「Y 軸の定数線」→ **`37.5`**（黄 `#F39C12`）、**`38.5`**（赤 `#E74C3C`）

#### ビジュアル C: SpO2 の時系列推移（折れ線グラフ）

1. キャンバスの空白部分をクリック
2. 「ビジュアルの種類」から **「折れ線グラフ」** を選択
3. フィールドを設定:
   - **X 軸**: `timestamp`
   - **Y 軸**: `spo2`（▼ → **「平均」** に変更）
   - **凡例**: `deviceId`
4. 心拍数グラフの下に配置（幅:半分、高さ:1/3）
5. 参照線を追加:「さらに分析をビジュアルに追加する」→「Y 軸の定数線」→ **`95`**（黄 `#F39C12`）、**`90`**（赤 `#E74C3C`）

#### ビジュアル D: デバイスフィルター（スライサー）

スライサーを追加すると、クリックでデバイスを絞り込めるようになります。

1. キャンバスの空白部分をクリック
2. 「ビジュアルの種類」から **「スライサー」** アイコン（フィルター漏斗のような形）をクリック
3. **`deviceId`** をドラッグして「**フィールド**」エリアにドロップ
4. ページ上部に横長に配置（幅:100%、高さ:小さく）
5. スライサーの **「ビジュアルの書式設定」** → **「スライサーの設定」** → スタイルを **「タイル」** に変更すると、ボタン形式で見やすくなります

> **動作確認**: スライサーで「icu-device01」をクリックすると、同じページの3つの折れ線グラフがすべて `icu-device01` のデータだけにフィルターされます。

**ページ 1 の完成イメージ:**
```
┌──────────────────────────────────────────────────┐
│  [icu-device01] [icu-device02] [general-01] ...  │ ← スライサー
├────────────────────────┬─────────────────────────┤
│  心拍数 (折れ線)       │  体温 (折れ線)          │
│  ──────────── 120      │  ──────────── 38.5      │
│  ──────────── 100      │  ──────────── 37.5      │
├────────────────────────┼─────────────────────────┤
│  SpO2 (折れ線)         │  （空きスペース or 追加）│
│  ──────────── 95       │                         │
│  ──────────── 90       │                         │
└────────────────────────┴─────────────────────────┘
```

---

### ページ 2: 異常検知サマリー

1. ページ下部のタブ横にある **「＋」ボタン** をクリックして新しいページを追加
2. 新しいタブを右クリック →「名前の変更」→ **「異常検知サマリー」**

#### ビジュアル E: ステータス分布（ドーナツチャート）

1. 「ビジュアルの種類」から **「ドーナツ グラフ」** （🍩のような円形）をクリック
2. フィールドを設定:
   - **凡例**: `serverStatus` をドラッグ
   - **値**: `serverStatus` をドラッグ（自動的に「serverStatus のカウント」になる）
   > **注意**: 凡例と値の**両方**に `serverStatus` を入れてください。値に入れないとドーナツが表示されません。
3. 左上に配置（幅:1/3、高さ:1/2）
4. **色の変更**:
   - グラフをクリック → 右側の **「ビジュアルの書式設定」**（ペイントローラーアイコン）
   - **「データの色」** (または「色」) を展開
   - `normal` → **`#2ECC71`** (緑)
   - `warning` → **`#F39C12`** (オレンジ)
   - `critical` → **`#E74C3C`** (赤)

#### ビジュアル F: デバイス別ステータス（積み上げ横棒グラフ）

1. キャンバスの空白部分をクリック
2. 「ビジュアルの種類」から **「積み上げ横棒グラフ」** をクリック
3. フィールドを設定:
   - **Y 軸**: `deviceId`
   - **X 軸**: `serverStatus`（ドロップすると自動で「serverStatus の数」になる）
   - **凡例**: `serverStatus`
4. ドーナツの右隣に配置
5. 色の変更: ドーナツと同じ（normal=緑, warning=オレンジ, critical=赤）

#### ビジュアル G: 時間帯別異常件数（積み上げ縦棒グラフ）

1. キャンバスの空白部分をクリック
2. 「ビジュアルの種類」から **「積み上げ縦棒グラフ」** をクリック
3. フィールドを設定:
   - **X 軸**: `timestamp`
   - **Y 軸**: `serverStatus`（自動で「数」になる）
   - **凡例**: `serverStatus`
4. 下部に幅広く配置
5. **warning + critical のみにフィルター**:
   - このグラフを選択した状態で
   - 右側の **「フィルター」** パネルを確認
   - 「このビジュアルのフィルター」に `serverStatus` をドラッグ
   - **`warning`** と **`critical`** に✓を入れ、`normal` の✓を外す
6. **色の変更**: ドーナツ・横棒グラフと統一
   - `warning` → **`#F39C12`** (オレンジ)
   - `critical` → **`#E74C3C`** (赤)

#### ビジュアル H: KPI カード（4枚）

**カード 1: 総メッセージ数**
1. キャンバスの空白部分をクリック
2. 「ビジュアルの種類」から **「カード」**（数字が1つ大きく表示されるアイコン）をクリック
3. **`deviceId`** を **「値」** エリアにドラッグ（「カテゴリ」ではなく **「値」** に入れること）
4. 「値」エリアの `deviceId` の横の **▼** をクリック → **「カウント」** を選択
5. 小さいサイズにして上部に配置

**カード 2: Critical 件数**
1. 空白をクリック → 「カード」を選択
2. `serverStatus` を **「値」** エリアにドラッグ → ▼ → **「カウント」**
3. **「フィルター」** パネルの「このビジュアルのフィルター」に `serverStatus` をドラッグ
4. **`critical`** のみ✓を入れる
5. カード 1 の横に配置

**カード 3: 平均心拍数**
1. 空白をクリック → 「カード」を選択
2. `heartRate` を **「値」** エリアにドラッグ → ▼ → **「平均」** を選択
3. カード 2 の横に配置

**カード 4: 平均 SpO2**
1. 空白をクリック → 「カード」を選択
2. `spo2` を **「値」** エリアにドラッグ → ▼ → **「平均」** を選択
3. カード 3 の横に配置

**ページ 2 の完成イメージ:**
```
┌───────────┬───────────┬───────────┬───────────┐
│ 総件数    │ Critical  │ 平均心拍   │ 平均SpO2  │ ← カード×4
│   3,240   │    162    │   78.5    │   96.2    │
├───────────┴──┬────────┴───────────┴───────────┤
│ ステータス   │ デバイス別ステータス             │
│  分布 🍩     │ icu-01   ████████░░░            │
│  normal 85%  │ icu-02   █████████░             │
│  warning 12% │ gen-01   ██████████             │
│  critical 3% │ gen-02   ██████████             │
│              │ gen-03   ████████░░             │
├──────────────┴─────────────────────────────────┤
│ 時間帯別異常件数 (積み上げ縦棒)                 │
│ ▓▓░░▓░░▓▓░░▓▓░░▓░░░▓▓░░                      │
└────────────────────────────────────────────────┘
```

---

### ページ 3: デバイス詳細

1. **「＋」** ボタンで新しいページを追加
2. タブを右クリック → 名前を **「デバイス詳細」** に変更

#### ビジュアル I: バイタルサイン一覧テーブル

1. 「ビジュアルの種類」から **「テーブル」**（格子状のアイコン）をクリック
2. 以下のフィールドを順番にドラッグして「**列**」エリアにドロップ:
   - `timestamp`
   - `deviceId`
   - `heartRate`（▼ → **「集計しない」** を選択）
   - `bodyTemperature`（▼ → **「集計しない」** を選択）
   - `spo2`（▼ → **「集計しない」** を選択）
   - `serverStatus`
   > **注意**: 数値フィールドはデフォルトで「合計」になり、ヘッダーに「heartRate の合計」と表示されてしまいます。テーブルでは個別レコードの値をそのまま表示したいので、**「集計しない」** に変更してください。
3. キャンバス全体に大きく配置

**並び替え:**
- テーブル内の `timestamp` 列ヘッダーをクリックすると昇順/降順が切り替わります
- 降順（新しい順）にしてください

**条件付き書式（serverStatus に色を付ける）:**
1. テーブルを選択した状態で
2. 右側パネルの **「ビジュアルの書式設定」**（ペイントローラー 🖌️ アイコン）をクリック
3. **「ビジュアル」** タブ → **「セルの要素」** を展開
4. **「系列」** ドロップダウンで **`serverStatus`** を選択
5. **「背景色」** のトグルを **オン** → 横の **「fx」** ボタンをクリック
6. 書式設定ダイアログが開く:
   - 「書式設定スタイル」: **「ルール」** を選択
   - 「フィールドに基づく」: `serverStatus`
   - ルール 1: 値が **「critical」に等しい** → 色 **`#E74C3C`** (赤)
   - **「+ 新しいルール」** をクリック
   - ルール 2: 値が **「warning」に等しい** → 色 **`#F39C12`** (オレンジ)
7. 「OK」をクリック

#### ビジュアル J: 血圧チャート（折れ線グラフ）

1. テーブルの下部に空きスペースを残しておく
2. 空白をクリック → 「折れ線グラフ」を選択
3. フィールドを設定:
   - **X 軸**: `timestamp`
   - **Y 軸**: `bloodPressureSystolic` をドラッグ、続けて `bloodPressureDiastolic` もドラッグ（2つのラインが表示される）。それぞれの **▼** → **「平均」** に変更
   > **注意**: Y 軸に複数フィールドを入れると、凡例は自動的に系列名（収縮期/拡張期）の区別に使われるため、`deviceId` は凡例に追加できません。デバイスの絞り込みはページ上部のスライサーで行ってください。
4. テーブルの下に配置
5. **参照線の追加**: 🔍 →「Y 軸の定数線」→「+ 行の追加」
   - **`140`** → 色 **赤 `#E74C3C`** → ラベル「高血圧(収縮期)」
   - **`90`** → 色 **赤 `#E74C3C`** → ラベル「高血圧(拡張期)」

#### スライサー（デバイスフィルター）

ページ 3 にもスライサーを追加しておくと便利です:

1. 空白をクリック → 「スライサー」を選択
2. `deviceId` を「フィールド」にドラッグ
3. ページ上部に横長に配置

---

## 手順 3: 書式設定のヒント

### 色の一覧

| ステータス | 色 | Hex | 用途 |
|---|---|---|---|
| normal | 緑 | `#2ECC71` | ドーナツ、棒グラフ |
| warning | 黄/オレンジ | `#F39C12` | ドーナツ、棒グラフ、参照線 |
| critical | 赤 | `#E74C3C` | ドーナツ、棒グラフ、参照線 |

### グラフのタイトルを変更する方法

1. グラフを選択
2. 右側の **「ビジュアルの書式設定」** （ペイントローラーアイコン）
3. **「全般」** タブ → **「タイトル」** を展開
4. テキスト欄にタイトルを入力（例: 「心拍数の推移」）

### ダッシュボード全体のタイトル

1. リボンメニューの **「挿入」** → **「テキスト ボックス」**
2. **「IoT 医療デバイス監視ダッシュボード」** と入力
3. フォントサイズ: 20〜24、フォント: 游ゴシック or メイリオ
4. ページ上部中央に配置

---

## 手順 4: 保存

1. **ファイル → 名前を付けて保存**
2. ファイル名: `IoT-Medical-Dashboard.pbix`
3. 保存先: `powerbi/` フォルダ

---

## デバイス一覧（参考）

| deviceId | 役割 | 送信間隔 | 特徴 |
|---|---|---|---|
| icu-device01 | 重症患者 | 10秒 | warning 20%, critical 5% |
| icu-device02 | 安定回復中 | 10秒 | warning 5%, critical 0.3% |
| general-device01 | 一般入院 | 30秒 | warning 8%, critical 0.5% |
| general-device02 | 経過観察 | 30秒 | warning 3%, critical 0.1% |
| general-device03 | 容態悪化傾向 | 30秒 | warning 15%, critical 3% |

## ステータス判定閾値（参考）

| バイタル | warning | critical | チェック方向 |
|---|---|---|---|
| heartRate | > 100 bpm | > 120 bpm | 上限超過 |
| bodyTemperature | > 37.5 ℃ | > 38.5 ℃ | 上限超過 |
| spo2 | < 95 % | < 90 % | 下限割れ |
